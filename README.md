# PSR
```
powershell reverse shell tool
```
what is this tool:
   
    This is a Simple Powershell Reverse shell file made with python and powershell

Prerequisites :

    nc (netcat) (for listener)
    python (for running tool)

run:

    python psr.py
